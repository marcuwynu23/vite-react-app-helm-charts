apiVersion: v1
kind: Service
metadata:
  name: {{ include "vite-app.fullname" . }}
  labels:
    {{- include "vite-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vite-app.selectorLabels" . | nindent 4 }}
