apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vite-app.fullname" . }}-test-connection"
  labels:
    {{- include "vite-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vite-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
